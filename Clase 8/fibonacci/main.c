#include <stdio.h>
#include <stdlib.h>

int fibonacci(int n)

int main()
{
    int num, res;
    printf ("Ingresa el numero de la serie de fibonacci que deseas calcular:\n\n");
    scanf("%i",&num);
    res=
}

int fibonacci (int n)
{
    if (n<=1)
        return n;
    else
        return fibonacci (n-1)+ fibonacci(n-2)
}
